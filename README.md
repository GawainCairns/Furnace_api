# Furnace API

Minimal Node.js project scaffold for the Furnace API.

Quick start

1. Install dependencies:

```bash
npm install
```

2. Run in development (requires `nodemon`):

```bash
npm run dev
```

3. Run in production:

```bash
npm start
```

The server will listen on `PORT` or `3000` by default.
